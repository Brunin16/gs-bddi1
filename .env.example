# Diretório raiz montado no container (padrão definido no docker-compose.yml)
# Altere apenas se usar um caminho personalizado.
ORBITALFIRE_HOME=/opt/airflow

# Credenciais Oracle: configuradas via Airflow Connections (não via variável de ambiente).
# Consulte o README, seção "Criar a Connection do Oracle".
